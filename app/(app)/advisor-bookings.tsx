import { View, Text } from "react-native";

export default function AdvisorBookings() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
        <Text>Advisor — My Bookings</Text>
        </View>
    );
}
