import { View, Text } from "react-native";

export default function ContactRequests() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
        <Text>Staff — Contact Requests</Text>
        </View>
    );
}
