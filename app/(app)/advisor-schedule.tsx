import { View, Text } from "react-native";

export default function AdvisorSchedule() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
        <Text>Advisor — My Schedule</Text>
        </View>
    );
}
